backend:
  name: git-gateway
  branch: tour # Branch to update (optional; defaults to master)
media_folder: assets/cartoons/
public_folder: cartoons/
collections:
  - name: 'tours'
    label: 'Tour Dates'
    folder: 'content/home/tour/'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title/Location - required', name: 'title', widget: 'string' }
      - { label: 'Description of the event (not required)', name: 'description', widget: 'string', required: false }
      - { label: 'Ticket purchase link', name: 'purchase_link', widget: 'string' }
      - { label: 'Event date - required', name: 'date', widget: 'datetime', date_format: "MM.DD.YYYY" }
      - { label: 'Sold Out? - required, default false', name: 'sold_out_boolean', widget: 'boolean', default: false }      
      - { label: 'Custom sold out description - not required', name: 'sold_out_description', widget: 'string', required: false }
  - name: 'cartoons'
    label: 'Cartoons'
    folder: 'content/home/cartoons/'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Image', name: 'image', widget: 'image' }
      - { label: 'Quick description of the action or premise of the image', name: 'alt', widget: 'string' }
      - { label: 'Date (Images are ordered newest to oldest)', name: 'date', widget: 'datetime' }
