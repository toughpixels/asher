body {
  background: pink;
  font-size: 20px;
  display: flex;
}

section {
  min-height: 100vh;
  position: relative;
}

header {
  height: 100vh;
}

.pageNav li:not(.active) {
    background: transparent !important;
  }

.pageNav li.active a {
    --tw-text-opacity: 1;
    color: rgba(29, 31, 32, var(--tw-text-opacity));
  }